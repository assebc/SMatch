import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("window");

export const SIZES = {
    width,
    height,
};

export const COLORS = {
    primary: "E8F3D6",
    secondary: "FCF9BE"
};
